# SNI Core Generator — Local Processing (No API)
pymupdf>=1.23.0       # fast PDF text extraction (fitz)
pypdf>=4.0.0          # fallback PDF parser
python-docx>=1.1.0    # Word document parser
streamlit>=1.35.0     # web UI
